---
nginx:
  prefix:
    config: /usr/local/etc/nginx
    log: /var/log/nginx
  nameservers:
    - 1.1.1.1
    - 1.0.0.1
  dhparam_bits: 4096
  moved_permanently: {}
  hsts:
    max_age: 31536000
    include_subdomains: no
    preload: no
  log_format: main
